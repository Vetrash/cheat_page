<template>
<h2>Какие теги используются для создания таблиц</h2>
<p>Для создания таблиц в HTML используются теги {{ `<table>, <tr>, <th> и
<td>` }} . Тег {{ `<table>` }}  создает таблицу, {{ `<tr></tr>` }} определяет строку в
таблице, {{ `<th>` }} задает заголовок ячейки, а {{ `<td>` }} создает ячейку в
таблице. Вместе эти теги позволяют структурировать данные в виде
таблицы.</p>

</template>

<script>
export default {};
</script>
